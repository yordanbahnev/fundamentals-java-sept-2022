package demo;

import java.sql.Array;
import java.util.Arrays;
import java.util.Scanner;

public class demo1 {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

//        String lineOfNumbers = scanner.nextLine();
//        String[] numbersAsStrings = lineOfNumbers.split(" ");
//        int[] numbers = new int[numbersAsStrings.length];
//        for (int i = 0; i < numbers.length; i++) {
//            numbers[i] = Integer.parseInt(numbersAsStrings[i]);
//        }
//        for (int i = 0; i < numbers.length / 2; i++) {
//            int otherIndex = numbers.length - 1 - i;
//            int currentNumber = numbers[i];
//            numbers[i] = numbers[otherIndex];
//            numbers[otherIndex] = currentNumber;
//        }
//        for (int i = 0; i < numbers.length; i++) {
//            System.out.println(numbers[i] + " ");
//            String values = scanner.nextLine();
//            int [] numbersArr = Arrays.stream(values.split(" ")).mapToInt(Integer::parseInt).toArray();
//        for (int i = 0; i < numbersArr.length; i++) {
//            System.out.printf("%d -> %d", i, numbersArr[i]);

    }
}






